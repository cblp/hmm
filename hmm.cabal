cabal-version: 2.2

name: hmm
version: 0

build-type: Simple

executable hmm
  build-depends:
    apecs,
    apecs-gloss,
    base,
    gloss,
    linear,
    random,
    vector,
    aeson-tiled
  main-is: Main.hs
