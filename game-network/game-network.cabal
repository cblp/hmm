cabal-version: 2.4

name: game-network
version: 0.1.0

build-type: Simple

library
  build-depends:
    base,
    bytestring,
    network,
    serialise,
  ghc-options:
    -Wall
  default-language: Haskell2010
  exposed-modules:
    Game.Network
    Game.Message
